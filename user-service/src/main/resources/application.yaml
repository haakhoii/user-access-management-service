server:
  servlet:
    context-path: /api/v1/user

spring:
  application:
    name: user-service

  profiles:
    active: ${SPRING_PROFILES_ACTIVE}

jwt:
  signerKey: ${JWT_SIGNER_KEY}
  expiry: ${JWT_EXPIRY}